from _typeshed import Self
from typing import Sized


class ProductItems:
    """ objeto incial de recogida de datos para publicaciones
    :methods __init__
    
    parameters: ordern de argumentos
    """
    
    def __init__(self, **kwargs) -> None:
        
        self.title = str(title)
        self.size = str(size)
        self.sku = int(sku)
        self.desc = str(desc)
        self.comp = str(comp)
        
        pass